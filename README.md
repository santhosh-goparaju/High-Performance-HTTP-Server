# High-Performance-HTTP-Server
A client connects over TCP, sends a valid HTTP request, the server parses it, routes it to a handler and returns a standards-compliant response. The server must handle many concurrent connections efficiently and provide reproducible performance measurements.
